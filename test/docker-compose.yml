version: "3.9"
services:
  ssh:
    build: docker
    ports:
      - "127.0.1.1:2222:2222"
    init: true

  ssh2:
    build: docker
    ports:
      - "127.0.1.1:2223:2222"
    init: true

  ssh3:
    build: docker
    ports:
      - "127.0.1.1:2224:2222"
    init: true
